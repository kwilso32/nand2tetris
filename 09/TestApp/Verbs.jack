class Verbs{
  field String name;
  field Array verbs;
  field int count;
  constructor Verbs new(String verbName, int length){
    let name = verbName;
    let count = 0;
    let objects = Array.new(length);

  }
  method Array getVerbs(){
    return verbs;
  }
  method void addVerb(String verb){
    verbs[count] = verb;
    let count = count + 1;
  }
}
