class Main {
  function void main(){
   do Main.out();
   return;
  }
  
  function void out(){
	var String line;
	var String line2;
	var boolean bool;
    let line = Input.readLine("Enter: ");
	let line2 = Input.readLine("Enter: ");
	let bool = Compare.compare(line, line2);
	if(Compare.compare(line, line2)){
		do Output.printString("Success");
	} else {
		do Output.printString("Fail");
	}
	return;
	
	}
  
 
}
